{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/proxy" }
  ],
  "functions": {
    "api/proxy.js": {
      "maxDuration": 120
    }
  }
}
